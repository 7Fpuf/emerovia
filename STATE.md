# Agent Commons — Current State

Updated by the builder loop. Newest entries at the bottom.

## Status
- 2026-09-22 23:05 CDT: Workspace created. PLAN.md written. Stage 1 build starting.

## Build queue
1. [DOING] Stage 1 foundation server: registration, ed25519 auth, signed chat,
   design-room proposals, read-only human view, tests.
2. [TODO] Persistent run: server running on builder VM, health checks.
3. [TODO] Python SDK for agents (register + chat in <10 min).
4. [TODO] Scheduled builder loop (cron) — created only after first run is verified.
5. [TODO] Public deployment decision (VPS + domain) — needs Trevor's approval + funding.
6. [TODO] Stage 2 world map spec.

## Progress log
- 2026-09-22 23:05 CDT: Project initialized in Muse. Stage 1 coordinator spawned.
- 2026-09-23 00:07 CDT: **Stage 1 foundation server complete and verified.** Built by 3 worker subagents under a coordinator.
  - `server/app.py` (FastAPI + SQLite, deps: fastapi, uvicorn, pynacl, httpx, pytest): POST /register (ed25519 hex pubkey, unique names, 409/400 handling); ed25519 signed-request auth (headers X-Agent-Pubkey / X-Timestamp / X-Signature over `timestamp\nMETHOD\npath\nbody`, 300s window, 401 + `agent_commons.auth` log line on unsigned/unknown/expired/forged); signed POST /chat (room defaults to "general", signatures stored); GET /chat (room/since/limit 100); signed POST /proposals (state always "open"); GET /proposals, GET /proposals/{id}; GET /agents; GET /health (counts); GET / serves static human view.
  - `server/static/index.html`: read-only observer view — fetches only GET /agents, /chat, /proposals, 15s auto-refresh, XSS-safe (textContent only), no <form>, no mutating methods (grep-verified).
  - `tests/test_stage1.py`: 10 pytest tests, all PASSING (verified by coordinator: `10 passed in 0.99s`): registration uniqueness, signed chat round-trip, tampered/expired/unknown/unsigned rejections, proposal lifecycle, read-only HTML check, persistence across app reload.
  - `scripts/run.sh` (uvicorn 127.0.0.1:8765) and `scripts/demo.py` (2 agents register, signed chat exchange, signed proposal, exit 0) both verified live by coordinator; server stopped after, port closed, no public exposure.
  - Note: `.venv/` created in project root for deps. VM quirk: `no_proxy` contains IPv6 bracket entries, so localhost HTTP clients need `trust_env=False` (fixed in demo.py).
- 2026-09-22 23:10 CDT: Stage 1 COMPLETE and verified. FastAPI+SQLite server (server/app.py), ed25519 signed-request auth, signed chat, design-room proposals, read-only human view (server/static/index.html). 10/10 pytest tests pass (verified independently by Mini). Server running persistently on 127.0.0.1:8765 (health: ok). No public exposure.
- 2026-09-22 23:13 CDT: Stage 2 build started (world coordinator). Recurring builder loop installed (every 6h, first run ~05:12 CDT); cron verification runonce fires 23:16 CDT.
- 2026-09-22 23:16 CDT: Scheduling VERIFIED (cron-ok heartbeat landed 23:16:03). Builder loop (6h) and world heartbeat (15m) both confirmed live. SDK + docs/JOIN.md built and tested by Mini directly. Stage 3 proposal-pipeline spec written. Launch-kit drafts delegated (nothing to be published without Trevor).
- 2026-09-22 23:17 CDT: Stage 2 COMPLETE and verified. Shared 64x64 world (server/world.py): seeded deterministic terrain (seed "agent-commons-genesis-v1": ocean 973, plains 850, forest 796, desert 838, mountain 639), signed endpoints /world/spawn|move|me|disclose|map|info|agents. Server-side AP: start 50, cap 100, +1/60s, land 1 AP, mountain 2 AP, disclose 1 AP, ocean impassable. Private discovery logs; public map only shows disclosed tiles. 23/23 tests pass (10 Stage 1 + 13 Stage 2), verified independently by Mini. demo_world.py ran end-to-end on scratch DB: 2 agents spawned/wandered/disclosed, AP math correct. Human view gained a World tab (canvas map, legend, agent markers, read-only). No public exposure.
- 2026-09-22 23:19 CDT: STAGE 2 COMPLETE. 64x64 genesis world live on 127.0.0.1:8765 (seed agent-commons-genesis-v1, 4096 terrain tiles: ocean 973, plains 850, forest 796, desert 838, mountain 639). Server restarted to pick up world endpoints; Mini verified 23/23 tests passing + demo_world.py end-to-end (2 agents spawned, moved, disclosed; AP math exact). Demo data cleared — world is pristine (0 agents, 0 messages, 0 proposals, 0 disclosed). SDK world methods (spawn/move/disclose/public_map) now testable against live server.
- 2026-09-22 23:20 CDT: Trevor named the world **Agentia**. Public-facing docs (JOIN.md, launch-kit drafts) now call the world Agentia; Agent Commons stays as the internal build codename. Observer-view title rename deferred to avoid clobbering the Stage 3 human-view worker.
- 2026-09-22 23:22 CDT: Stage 3 COMPLETE (coordinator-verified). Proposal pipeline live: signed comments (POST/GET /proposals/{id}/comments), operator-only PATCH /proposals/{id}/state (open→discussing→accepted|rejected→in_test→merged; agent keys → 403, invalid → 400), public append-only GET /operator-log with genesis entry, test_report attachment on in_test/merged, proposal detail + operator-log tab in human view (read-only, grep-verified). Tests: 31/31 pass (10 stage1 + 13 stage2 + 8 stage3). scripts/demo_stage3.py exits 0. server/operator.key generated, mode 600, never exposed. Live DB migrated idempotently. No code path interprets proposal bodies — inert text only.
- 2026-09-22 23:40 CDT: Agent-side onboarding package COMPLETE. (1) SDK packaged: sdk/pyproject.toml (dist name agentia-sdk 0.1.0, MIT, requires pynacl; py-modules keeps `import agent_commons_sdk` working) + sdk/README.md; verified via fresh venv `pip install ./sdk` and import. Added additive SDK methods: get_proposal, comment, proposal_comments, operator_log, world_agents. (2) docs/JOIN.md rewritten for agent readers: terse imperative, exact auth spec (headers X-Agent-Pubkey/X-Timestamp/X-Signature, payload ts\nMETHOD\npath\nbody, 300s window), full endpoint list verified against server/app.py, no invented endpoints. (3) server/static/agents.txt (32 lines, [SERVER_URL] placeholder) + new read-only GET /agents.txt route (static wasn't mounted). Live-verified: /agents.txt → 200, SDK comment round-trip against live server works. Tests: 31/31 pass. Nothing published to PyPI (founder approval required). Localhost only.

## 2026-09-22 23:46 CDT — Agent outreach playbook (research only, no execution)
- Researched Moltbook live: skill.md/docs pattern, register->claim_url->tweet claim flow (1 agent per X account), endpoints, rate limits (1 post/30min est., 50 comments/day), new-agent reverse CAPTCHA (/verify within 5 min), rules.md (excessive self-promotion=warning, spam=ban), live-verified submolt list (introductions 140k subs, general 140k, agents 3.7k, builds 2.5k). Meta acquired Moltbook Mar 2026; ~207k human-verified agents (Jun 2026).
- Mapped secondary venues: ClawHub skill marketplace, Molthunt, agent-friendly-directory, AICQ, The Colony/Agentchan/Agent Arena, Nostr, 4claw.org, OpenClaw Discord + X (human operators).
- Verified agent-to-agent discovery chain is feasible: agents poll feeds/browse web, read skill.md-style docs, fetch agents.txt, POST-register autonomously.
- Drafted docs/launch-kit/agent-outreach-playbook.md: founder claim steps, outreach-agent disclosure standards (one disclosed voice, no astroturfing), 4-phase Moltbook campaign, 3 example disclosed posts, ClawHub/Molthunt/directory plan, human-side X/Reddit/Discord amplification, 10 hard "what NOT to do" lines. DRAFT — nothing executed, no accounts, no posts, no contact.
## 2026-09-22 23:52 CDT — Town crier network addendum (research only, no execution)
- Appended "Town crier network" (§10) to docs/launch-kit/agent-outreach-playbook.md: medium-to-platform map (conversational crier vs skill vs listing vs human-side), per-platform crier specs for The Colony (two-call registration, JWT, SDKs, ~400 agents, karma trust tiers, no human verification), AICQ (Ed25519 POST /register, rooms/DMs, 4KB msg / 32KB-per-min limits, aicqsdk), Nostr (zero-signup secp256k1 keypair, kind-0 profile disclosure, kind-1 notes, announcements-only cadence), ClawHub skill publish spec (clawhub login via GitHub, skill publish --slug agentia, scanner compliance), Molthunt + agent-friendly-directory one-time listings, and explicit no-crier calls for 4claw (anti-promo culture), Agentchan (anonymous — disclosure impossible), Agent Arena (commercial venue).
- Hard rules repeated per platform: one disclosed crier each, full affiliation disclosure, no sockpuppets/fake independence/spam/vote manipulation, obey host rules, founder approves every phase, no token talk, credential hygiene, no ban evasion.
- Recommended rollout order: 1 Moltbook crier, 2 ClawHub agentia skill, 3 The Colony crier, 4 Molthunt + directory listings, 5 AICQ crier, 6 Nostr broadcast, 7 deferred (4claw/Agentchan/Agent Arena).
- DRAFT only — no accounts, no posts, no contact, no spending. Placeholders [SERVER_URL], [FOUNDER_X_HANDLE], [AGENT_NAME] still unfilled.

## 2026-09-23 ~04:58 UTC — Pre-launch game audit (agent's-eye playthrough)
- Played end-to-end as an agent via packaged SDK: register, chat, spawn, ~15 moves, disclose, proposals, comments, operator log, directory. Probed spam/abuse edges.
- Findings doc: docs/prelaunch-game-audit.md — 6 Tier-A items (no rate limiting; SDK generate() overwrites identity → permanent lockout; no proposal endorsements; operator-only state bottleneck; disclosures earn zero credit; no progression/retention loop), 6 Tier-B, 6 Tier-C roadmap.
- Validation/error messages all correct; auth held up (bad sig/expired/unknown pubkey → 401; ocean impassable).
- Audit agents (audit-scout, audit-scout-2), their messages/proposals/comments/world rows, and disclosed tile (39,39) removed via DB; identity files deleted. DB restored to pre-audit state (4 agents, 3 msgs, 2 proposals, 4 public tiles). No server code modified.

## 2026-09-23 ~05:10 UTC — Tier-A pre-launch items implemented (6/6)
- A1 Rate limiting: per-agent fixed windows in new `rate_limits` table — chat 1/5s, comments 1/10s, proposals 3/hr, endorsements 10/min. 429 + Retry-After header. Checked after auth+validation, inside the write txn (rolls back if the write fails).
- A2 SDK `Agent.generate(name)` now raises FileExistsError if the identity file exists; explicit `force=True` regenerates (old key discarded, loudly). `Agent.load()` unchanged.
- A3 Endorsements: signed `POST /proposals/{id}/endorse` (201, 409 on repeat, 404 unknown proposal), signed `DELETE` to retract, public `GET /proposals/{id}/endorsements` ({count, [{agent_name, ts}]}). Endorsement counts added to `GET /proposals` and `GET /proposals/{id}` (additive field).
- A4 Agent-driven motion: 5 distinct endorsements auto-transitions open->discussing (ENDORSE_AUTO_DISCUSS_THRESHOLD=5), logged to operator_log with actor="agents", detail marked "(agent-driven)". Guarded UPDATE ... WHERE state='open' so it fires exactly once. Operator-only PATCH path untouched (403s preserved).
- A5 Disclosure attribution: `GET /world/map` tiles now include `disclosed_by` (agent name). Observer UI world tooltip shows "terrain · disclosed by <name>" on hover.
- A6 Leaderboard: public `GET /stats/leaderboard` (tiles_explored, tiles_disclosed, proposals_submitted/accepted, endorsements_given/received), sorted tiles_disclosed DESC, endorsements_received DESC, tiles_explored DESC. Observer UI has a Leaderboard section/table; proposal cards + detail show endorsement counts and endorser list.
- SDK: added `endorse()`, `retract_endorsement()`, `endorsements()`, `leaderboard()`. Backward compatible otherwise.
- Docs: docs/JOIN.md agent section + server/static/agents.txt updated (new endpoints, rate limits, threshold, generate safety).
- Tests: new tests/test_tier_a.py (14 tests). Existing test_stage2 assertion updated for disclosed_by field; test_stage3::test_invalid_state_transitions spreads 4 proposals across 2 keys (3/hr limit). Suite: 45 passed (31 existing + 14 new).
- Deliberately NOT implemented: public operator SLA (founder policy decision — open); registration cooldown/PoW (audit flagged as decide-with-founder); retractable endorsements beyond single DELETE (done: DELETE retracts); endorsement-driven motion beyond open->discussing (kept minimal per task).

## 2026-09-23 ~00:15 CDT — Launch readiness: demo-data inventory + launch checklist (research/doc only)
- Demo-data inventory (queried live from agent_commons.db): agents=4 (pathfinder, cartographer, opcheck, opcheck3), messages=3, proposals=2 ("Shared trail markers", "Night cycle for the map", both state=open), proposal_comments=1, endorsements=0, discoveries=10, public_map=4, agent_world=2, rate_limits=0, operator_log=1 (genesis entry "Agent Commons world initialized"), world_tiles=4096 seeded terrain (preserve).
- Wrote docs/launch-checklist.md: 9-phase runbook (pre-freeze verification incl. 12-point endpoint smoke list on scratch DB; demo wipe via DB delete + init_db re-seed with per-table disposition and verification SQL; operator-key handling incl. never-transfer-private-key rule; VPS provisioning/hardening; domain+DNS+TLS; deploy incl. systemd + nginx + venv test on VPS; 13-point post-deploy smoke against public URL; launch-kit placeholder fill + approval-gated announcement/outreach order; 24h watch). [FOUNDER] gates marked on: provider/payment, account email/2FA, snapshot costs, domain choice/purchase/DNS registrar actions, Moltbook claim tweet, all outreach phases, operator-key backup holder, and any spend.
- server/operator.key confirmed present, mode 600. No code changes, no deploys, no DNS, no outreach executed — localhost only.

## 2026-09-23 ~05:15 UTC — Scale plan drafted ("biggest thing ever" directive)
- Wrote docs/scale-plan.md: tiered (100s → 10k → 1M+) plan covering data layer (SQLite→WAL→Postgres with concrete triggers), world size (procedural region expansion, Region 0 immutable), Sybil/abuse (staged: PoW-ready → trust tiers → vouch weighting → human-verified fallback; honest note that zero-human-input + Sybil-proof is unachievable simultaneously), read/write load (poll-first through 100k), governance evolution (operator-led → endorsement-weighted → delegated → constitutional, end state left for agents to propose), operations (backups/monitoring/runbook/cost tiers/sustainability without token), ecosystem (TS SDK first, OpenAPI, devrel-for-agents).
- Guiding principle: PLAN THE PATHS, DON'T BUILD THE HIGHWAYS. 10 tripwires with signals/responses; 6 do-now path-keeping changes (all small, for builder team). No code changed, no spending, no external contact.

## 2026-09-23 ~00:35 CDT — Tier-B triage (2/6 implemented) + scale path-keeping (2 items)

**Implemented B1 — chat room discovery.** `GET /chat/rooms` (public): rooms with messages, each `{room, message_count, last_message_at}`, ordered by most recent activity, cap 100. Rooms are implicit (posting creates them); empty rooms don't list. Zero state changes, backward compatible.

**Implemented B3 — agent profiles (bio).** `agents.bio` column (NULL default, guarded ALTER migration for live DBs — verified on production DB). Optional `bio` at `POST /register` (unsigned), signed `PATCH /agents/me` to set/update (`""` clears), 500-char max with precise 400s. `/agents` directory and register response now include `bio`. SDK: `Agent.register(base_url, bio=...)`, `update_profile(bio)`, `chat_rooms()`.

**Skipped (debatable, not cheap enough for day one):** B2 DMs/mentions — new tables + privacy semantics, medium effort; social coordination can start in rooms. B4 key rotation/unregister — B4's *key rotation* was replaced by a safer path: operator-key rotation implemented (see below); *agent* key rotation + unregister is a security-critical protocol change (old-key authorizes new-key, account deletion semantics), medium effort, deferred. B5 spawn preference — touches world engine fairness + migration, small–medium, day-one agents can just walk. B6 AP economy — design-heavy with no sinks designed; building one now risks bad economics.

**Scale path-keeping 1 — SQLite WAL + busy_timeout.** New `_configure_db()` helper: `PRAGMA journal_mode=WAL` + `PRAGMA busy_timeout=5000`, applied on every connection (init_db, create_app startup connections, and the per-request `connect()`). WAL persists on the live DB (verified `journal_mode=wal` on agent_commons.db). Invisible to API/docs.

**Scale path-keeping 2 — operator-key rotation (implemented, not just documented).** (a) Server: `authenticated_operator` now accepts a comma-separated list in `AC_OPERATOR_PUBKEY` (any listed key authenticates; single key works exactly as before — backward compatible; garbage entries fail closed, empty → 503). (b) New `scripts/rotate_operator_key.py`: generates fresh ed25519 pair, writes PRIVATE key to file only (mode 600 via os.open, path from argv, default `server/operator.key.new`), prints ONLY the new public key plus the exact 5-step rotation ceremony. Private key never printed/logged/transmitted (asserted in tests). (c) Docs: launch-checklist Phase 3 rotation subsection. Deferred per directive: db.py centralization, region_id (refactor/schema churn, zero day-one value).

- Docs: docs/JOIN.md agent section (endpoint reference + Profiles section) and server/static/agents.txt updated for B1/B3.
- Server restarted with AC_OPERATOR_PUBKEY to pick up changes; live endpoints verified (/health, /chat/rooms, /agents with bio:null on real data).
- Tests: new tests/test_tier_b.py (11: rooms empty/count/order/shape; bio register/roundtrip/null/reject/patch/clear/auth/long/migration) and tests/test_scale_path.py (8: WAL mode, busy_timeout, single-key works, comma-list accepts both, removed key 403s, whitespace tolerant, rotate script file-only + ceremony + default path). Suite: **70 passed** (45 pre-existing + 6 sibling day-one + 19 new).
- Note: mid-work bug (my own edit dedented create_app, making it return None) — caught by test run, fixed via re-indent, confirmed root-caused before green.

## 2026-09-23 ~00:50 CDT — Day-one experience pass (worker)
- Full new-agent walkthrough via packaged SDK + raw HTTP against live 127.0.0.1:8765: generate (FileExistsError on re-gen OK), register → 201, 409 on duplicate, chat → 201, 429 + Retry-After on burst, spawn/moves/disclose round-trip, endorse → 201 / 409 repeat / 404 unknown / retract → 200 / 404 repeat-retract, propose → 201, comment → 201 (+429 burst), leaderboard + operator-log reads. Auth edges: bad sig / expired ts / missing headers / unknown+malformed pubkey → 401 with correct details.
- Papercuts fixed: (1) GET /chat ignored the `limit` param that docs + SDK promised → added `limit` (default 100, clamped 1–100); (2) 429 detail "1 event(s) per 5 second(s)" → "rate limited: chat allows 1 per 5s" (kept "rate limited" substring tests assert); (3) SDK Agent.generate() now rejects names outside 1–64 chars client-side (server would 400); (4) docs clarified: /chat `since` = exclusive msg id + limit honored, /world/me requires spawn first (400 "not spawned"), leaderboard exact field names + sort order, /agents field list gained `id`, agents.txt FULL API gained GET /proposals/{id}.
- Docs-vs-reality audit (JOIN.md agent section + agents.txt vs final server/app.py + world.py): all endpoints/headers/field names/rate limits/thresholds verified match, incl. sibling's Tier-B additions (GET /chat/rooms fields+order, bio ≤500 at register/PATCH /agents/me/"" clears, operator-key comma list is operator-side only — agent docs correctly don't mention the env var; docs/launch-checklist.md already documents comma-separated rotation). world.py untouched by Tier-B/A-day-one work.
- Concurrency note: sibling (Tier-B) edited server/app.py, sdk/, docs mid-pass; one accidental duplicate SDK method pair (chat_rooms/update_profile) was deduplicated, keeping the sibling's versions. Final sweep done AFTER the sibling's Tier-B completion entry; server (pid 17857, started 05:03:52 UTC) runs the final code — live-verified /chat?limit=1 → 1 msg, /chat/rooms, bio roundtrip/clear/400-on-501-chars.
- Tests: 70/70 green (10 stage1 + 13 stage2 + 8 stage3 + 14 tier_a + 11 tier_b + 8 scale_path + 6 new tests/test_dayone.py: chat limit honored/default cap, 429 detail format, SDK name validation, /world/me-before-spawn 400).
- Cleanup: walkthrough agent/content removed from live DB (back to 4 agents, 3 msgs, 2 proposals, 4 public tiles, 1 operator-log genesis entry); ~/.agent-commons/dayone-walker.json deleted. No publishes, no deploys, no external contact.

## 2026-09-23 ~05:20 CDT — Persistent-run hardening (builder loop)
- Found world server DOWN again: 16+ clean-SIGTERM kills today (VM replacements + exec-session teardowns; systemd unavailable, no D-Bus). The 15m heartbeat was the only supervisor, leaving up-to-15-min gaps.
- **Bug fixed:** every heartbeat restart used plain `nohup uvicorn` with NO `AC_OPERATOR_PUBKEY`, so all restarts since 00:35 CDT left operator endpoints answering 503 ("operator not configured"). Verified on code + live behavior (random-key PATCH now returns 403, not 503).
- New `scripts/launch.sh`: idempotent launcher — pidfile-based (kills only the recorded PID, never pkill -f), derives AC_OPERATOR_PUBKEY from server/operator.key (public key only in env), hardcodes --host 127.0.0.1, setsid-detaches, waits for /health (30s), exits 0/1. Verified: launch ok, idempotent (pid unchanged when healthy), localhost-only bind confirmed via ss.
- New `scripts/supervise.sh [secs]`: 60s health-check loop for ~13 min (flock-serialized so overlapping runs exit), relaunches via launch.sh on failure, logs only state transitions to server/heartbeat.log, exits nonzero if server still down at end.
- Failure path tested live: killed server → supervise.sh detected failure in <60s → relaunch ok → health ok (agents=4, messages=3, proposals=2).
- Heartbeat cron body updated to run supervise.sh each 15m tick (worst-case downtime now ~1 min instead of ~15). Schedule/cadence untouched. Two-failures-in-a-row alert rule preserved.
- Full suite: 70/70 pytest pass after all changes. No public exposure, no token/money/promotion work, no external contact.

## 2026-09-23 ~06:57 CDT — Public name changed: Agentia → Emerovia
- Trevor approved the rename. Reason: agentia.com is premium/squatted, agentia TLDs taken, and an existing Australian financial firm operates as "Agentia" (trademark risk). ~65 candidate names checked via RDAP/DoH + web collision search; Mythia (registered US trademark), Solavia (UK/Belgium companies + US trademark), and Noavia (active skincare brand + German AI company) were killed. emerovia.com verified available (Cloudflare RDAP 404); NOT yet purchased — no spending approved.
- Renamed across repo: "Agentia"→"Emerovia", "agentia"→"emerovia", SDK dist name agentia-sdk→emerovia-sdk (pyproject.toml; `import agent_commons_sdk` unchanged). Internal codename "Agent Commons"/"agent_commons"/"agent-commons" untouched. server/static/index.html skipped (separate UI agent rewriting it with Emerovia branding). Historical log entries above keep the name "Agentia" as it was true at the time. Ticker: AGIA (under old name) → EMER now.
- Build artifacts regenerated: deleted sdk/agentia_sdk.egg-info/, sdk/build/, stale __pycache__; rebuilt egg-info for emerovia-sdk.

## 2026-09-23 ~07:10 CDT — Observer UI rebuilt as game-like spectator experience (worker)
- Rewrote server/static/index.html (was a static dashboard) into "Emerovia — Observer View": full-screen 60fps canvas world map, live event feed, agent spotlights, animated leaderboards, proposals, chat, operator log. Strictly read-only — no inputs, forms, or state-changing controls anywhere in the UI.
- Map: 64x64 terrain pre-rendered offscreen (per-tile hash shading/speckle, fog-of-war for undisclosed), agents as glowing avatars with smooth interpolated movement + fading trails + name labels, expanding ping/flash effects on new tile disclosures, vignette + scanline overlays. Camera: drag-pan, wheel-zoom (cursor-anchored), auto-follow-the-action mode (default ON, follows agent of latest event at 3.2x zoom), reset-view button, click-agent-to-spotlight hit-testing, hover HUD showing tile coords/terrain/discloser.
- Drama: big-moment banners (first terrain reveal, new proposal, state changes, 5-endorsement threshold), live feed with icons (🗺️💬📜👍🚶✨), agent spotlight cards (avatar, bio, position, 4-stat grid from leaderboard, recent activity, follow button), rank-change arrows + row flash on leaderboards (4 metric tabs), header counters (agents/tiles/messages/proposals) with count-up animation.
- Polling: /world/map + /world/agents every 3s, /chat (since=last id) 4s, /proposals 5s, /stats/leaderboard 7s, /agents + /health 8s, /operator-log 15s. First-load adopts existing state silently (no event storm); only genuinely new activity triggers feed/banners/effects.
- Branding: Emerovia throughout (title "Emerovia — Observer View", header, feed boot line). No "Agentia" remains in the file. Internal "Agent Commons" codename untouched (not referenced in UI).
- Verified: (1) tests/test_stage1.py 10/10 pass incl. test_human_view_is_read_only (no post/put/delete/patch/<form substrings; /agents /chat /proposals referenced); (2) tests/test_dayone.py + tests/test_tier_a.py 20/20 pass; (3) node --check on extracted script OK; (4) headless Node smoke test with DOM/canvas stubs + REAL fetches against live server: eval clean, 8 rAF frames, ~12s of live polling — feed populated (28→5 items after boot-silence fix), 4 agent cards, 6 rank rows, 14 chat msgs, 3 proposal cards, zero JS errors; (5) PIL re-render of the terrain math confirmed palette/fog/agent dots read well.
- Liveliness drill: registered 2 demo agents (ember-ui, moss-ui) via signed SDK-style requests; they spawned, wandered ~60 moves, disclosed tiles (4→23 public), chatted, proposed — all visible through the new UI's pollers. Demo data only; wiped at public launch per launch checklist.
- NOT verified visually: sandbox Chromium is a hardened build that hard-blocks all loopback navigation (ERR_BLOCKED_BY_LOCAL_NETWORK_ACCESS_CHECKS, immune to --disable-features flags), and the Playwright browser download failed — so no pixel screenshot of the real page was possible here. Parent should open http://127.0.0.1:8765/ in the live browser for a visual pass before showing Trevor.
- Cleanup: removed CDP chrome instances and the port-forwarder; world server (pid 2271) untouched and healthy.

## 2026-09-23 ~11:50 CDT — Stage 4 economy experiment COMPLETE (worker)
- **Economy engine** (`server/world.py`): TERRAIN_RESOURCE (plains→grain, forest→timber, mountain→ore, desert→glass; ocean none), deterministic per-tile stock 5..10 from `sha256("x,y,resource,agent-commons-genesis-v1")` via idempotent `seed_resource_stock()` (ocean gets no rows), `gather()` costing 2 AP — atomic AP-deduct + stock-decrement (guarded `UPDATE ... WHERE stock > 0`) + inventory increment; 400 ocean/nothing-to-gather, 400 depleted, 400 inventory cap (99), 402 insufficient AP (same convention as move/disclose, same 60s AP regen).
- **Server** (`server/app.py`): new tables `world_resource_stock`, `inventories`, `credit_balances`, `trade_offers`, append-only `trade_ledger` (no DELETE endpoint, no removal path). 100 valueless chits granted at `/register` (response includes `chits`) + idempotent backfill for pre-Stage-4 agents (INSERT OR IGNORE — never clobbers existing balances). Endpoints: signed `POST /world/gather`, signed `GET /world/inventory` (own only), signed `POST /trade/offers` ({give,want}, validation, maker must hold give, max 5 open/maker → 429), public `GET /trade/offers`, signed `POST /trade/offers/{id}/accept` (atomic both-sides-covered swap with rowcount guards, self-accept 400, filled/cancelled 409, taker/maker uncoverable 409, ledger row appended), signed `POST .../cancel` (maker only 403, open only 409), public `GET /trade/ledger` (oldest first, limit 1-100), public `GET /stats/economy` (trades_total, unique_traders, offers_open, volume_chits, volume_by_resource, total_stock_remaining). Rate limits (per-agent, 429 + Retry-After): offers 3/hr, accepts 10/min, gather 1/2s.
- **Chits policy** enforced in code + docs: valueless, non-redeemable, move ONLY through trade offers (no send endpoint), explicitly NOT crypto — no token/wallet/money work.
- **SDK** (`sdk/agent_commons_sdk.py`, `import agent_commons_sdk` unchanged): `gather()`, `inventory()`, `chits()`, `create_offer(give, want)`, `list_offers()`, `accept_offer(id)`, `cancel_offer(id)`, `trade_ledger(limit=100)`, `economy_stats()`; documented in sdk/README.md.
- **Docs**: docs/JOIN.md (economy endpoint block + rules + rate limits), server/static/agents.txt (FULL API + economy rules section), new docs/stage4-economy.md (experiment question, resource/trade rules, chits policy, REPORT TEMPLATE with blank tables — left blank until independent agents trade).
- **Tests**: tests/test_stage4.py, 27 tests (stock seed determinism/idempotency, gather terrain/AP/stock/inventory, ocean/unspawned/depleted/cap/AP/rate-limit edges, chits grant + backfill + no-clobber, offer create/validation/cap/rate-limit, accept atomicity + ledger + self/double/uncoverable + maker-drained, cancel auth/state, ledger shape/limit clamp, stats numbers incl. stock-decrement wiring). Full suite: **97/97 pass** (70 existing + 27 new).
- **Live migration verified** on agent_commons.db: tables created, 3,123 stock rows (4096−973 ocean), all 6 existing agents backfilled to 100 chits, 0 rows altered otherwise. Server restarted via scripts/launch.sh (stopped old pre-change PID via pidfile only — never pkill/session kill; new PID serves new code, localhost-only).
- **Live smoke** (2 temp agents, localhost): register→100 chits, spawn, gather glass on desert (stock 6→5), offer 1 glass for 5 chits → accept filled; ledger + /stats/economy correct; inventories swapped (105/95 chits, glass moved). All temp rows deleted afterward (agents, inventories, balances, offers, ledger, rate limits) + the 1 gathered glass unit restored to seed value — live DB back to pre-work state (6 agents, 14 msgs, 3 proposals, stock 23,340).
- Deliberately left out: no regrow mechanic (scarcity permanent by design), no public inventories (private by design), no direct chit transfers (by design), no offer expiry (not in spec).

## 2026-09-23 ~11:55 CDT — Stage 4 independently verified (Mini)
- Ran full suite myself: **97/97 pass** (70 pre-existing + 27 test_stage4), 18.65s.
- Live spot-checks on 127.0.0.1:8765: /health ok (6 agents, 14 msgs, 3 proposals — pristine, no temp rows); /stats/economy live with total_stock_remaining=23,340; /trade/ledger + /trade/offers empty as expected; DB shows 3,123 stock rows and all 6 agents at 100 chits.
- No code fixes needed; worker's implementation accepted as-is. Chits policy (valueless, non-redeemable, offer-only movement) confirmed in docs + code — no token/money boundary crossed.
- Next: Stage 4 report template stays blank until independent agents trade — requires launch/outreach (Trevor-gated).

## 2026-09-23 ~12:07 CDT — Legacy goal folded into Emerovia (Trevor: "Fold it in")
- Closed `goal_cbcd7f7d6f4b` ("Launch an open game world for AI agents with a Solana currency", formerly Agentia/AGIA).
- Salvaged into `docs/legacy/`: `world-design-notes.md` (region-graph model, livelihoods, skills, factions/karma, death rules, 36-action catalog), `api-onboarding-notes.md` (API sketch, MCP option, llms.txt onboarding, SDK/archetypes, trust & safety), `outreach-playbook.md` (positioning, personas, voice, good-citizenship rules, Moltbook beachhead, metrics — rebranded, gate corrected to Trevor), `tokenomics-reference.md` (faucet/sink model, quest thermostat, 5-layer anti-sybil, allocation, circuit breakers — marked SUPERSEDED/reference-only), plus `FOLD_IN.md` record.
- Left behind: AGIA identity/ticker, devnet token attempt (historical only), Solana-wallet identity (Emerovia uses Ed25519), phase-2 prototype code (superseded), "seed house agents" (conflicts with no-fake-population rule).
- Source archive retained at `~/workspace/goals/launch-an-open-game-world-for-ai-agents-with-a-solana-currency/`.
- Observer UI upgrade (world wire ticker, reveal streaks, records, fireworks, chat pings) pushed to public GitHub `7Fpuf/emerovia` main (commit b92bd486a1fa) via transient Trevor-supplied token; token to be deleted by Trevor.
